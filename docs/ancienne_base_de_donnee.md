-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public._prisma_migrations (
  id character varying NOT NULL DEFAULT (gen_random_uuid())::text,
  checksum character varying NOT NULL,
  finished_at timestamp with time zone,
  migration_name character varying NOT NULL,
  logs text,
  rolled_back_at timestamp with time zone,
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  applied_steps_count integer NOT NULL DEFAULT 0,
  CONSTRAINT _prisma_migrations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.academic_ledger (
  id text NOT NULL,
  schoolId text NOT NULL,
  entityId text NOT NULL,
  mutationId text,
  action USER-DEFINED NOT NULL,
  payloadBefore jsonb,
  payloadAfter jsonb NOT NULL,
  previousHash text NOT NULL DEFAULT 'GENESIS'::text,
  currentHash text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdBy text NOT NULL,
  resolvedBy text,
  CONSTRAINT academic_ledger_pkey PRIMARY KEY (id)
);
CREATE TABLE public.academic_years (
  id text NOT NULL,
  name text NOT NULL,
  start_date timestamp without time zone NOT NULL,
  end_date timestamp without time zone NOT NULL,
  is_current boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  school_id text NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT academic_years_pkey PRIMARY KEY (id),
  CONSTRAINT academic_years_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.access_profile_modules (
  id text NOT NULL,
  access_profile_code text NOT NULL,
  school_group_id text,
  module_id text NOT NULL,
  can_read boolean NOT NULL DEFAULT true,
  can_write boolean NOT NULL DEFAULT false,
  can_delete boolean NOT NULL DEFAULT false,
  can_export boolean NOT NULL DEFAULT false,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_by_id text,
  CONSTRAINT access_profile_modules_pkey PRIMARY KEY (id),
  CONSTRAINT access_profile_modules_accessProfileCode_schoolGroupId_fkey FOREIGN KEY (access_profile_code) REFERENCES public.access_profiles(code),
  CONSTRAINT access_profile_modules_accessProfileCode_schoolGroupId_fkey FOREIGN KEY (school_group_id) REFERENCES public.access_profiles(code),
  CONSTRAINT access_profile_modules_accessProfileCode_schoolGroupId_fkey FOREIGN KEY (access_profile_code) REFERENCES public.access_profiles(school_group_id),
  CONSTRAINT access_profile_modules_accessProfileCode_schoolGroupId_fkey FOREIGN KEY (school_group_id) REFERENCES public.access_profiles(school_group_id),
  CONSTRAINT access_profile_modules_moduleId_fkey FOREIGN KEY (module_id) REFERENCES public.modules(id)
);
CREATE TABLE public.access_profiles (
  id text NOT NULL,
  code text NOT NULL,
  label text NOT NULL,
  school_group_id text,
  description text,
  is_default boolean NOT NULL DEFAULT false,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  created_by_id text,
  CONSTRAINT access_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT access_profiles_schoolGroupId_fkey FOREIGN KEY (school_group_id) REFERENCES public.school_groups(id)
);
CREATE TABLE public.accounting_entries (
  id text NOT NULL,
  school_id text NOT NULL,
  academic_year_id text,
  entry_number text NOT NULL,
  journal_type USER-DEFINED NOT NULL,
  description text NOT NULL,
  reference text,
  debit_amount numeric NOT NULL DEFAULT 0,
  credit_amount numeric NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'XAF'::text,
  account_code text NOT NULL,
  account_name text NOT NULL,
  expense_id text,
  payment_id text,
  invoice_id text,
  budget_line_id text,
  payslip_id text,
  source_type text,
  fiscal_period text,
  batch_id text,
  is_reversing boolean NOT NULL DEFAULT false,
  reversedEntryId text,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"AccountingStatus",
  validated_by_id text,
  validated_at timestamp without time zone,
  entry_date timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT accounting_entries_pkey PRIMARY KEY (id),
  CONSTRAINT accounting_entries_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT accounting_entries_budgetLineId_fkey FOREIGN KEY (budget_line_id) REFERENCES public.budget_lines(id),
  CONSTRAINT accounting_entries_expenseId_fkey FOREIGN KEY (expense_id) REFERENCES public.expenses(id),
  CONSTRAINT accounting_entries_invoiceId_fkey FOREIGN KEY (invoice_id) REFERENCES public.invoices(id),
  CONSTRAINT accounting_entries_paymentId_fkey FOREIGN KEY (payment_id) REFERENCES public.payments(id),
  CONSTRAINT accounting_entries_payslipId_fkey FOREIGN KEY (payslip_id) REFERENCES public.payslips(id),
  CONSTRAINT accounting_entries_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.accounting_piece_lines (
  id text NOT NULL,
  pieceId text NOT NULL,
  accountCode text NOT NULL,
  accountId text,
  debitAmount numeric NOT NULL DEFAULT 0,
  creditAmount numeric NOT NULL DEFAULT 0,
  label text NOT NULL,
  costCenter text,
  cycleId text,
  lineOrder integer NOT NULL DEFAULT 0,
  CONSTRAINT accounting_piece_lines_pkey PRIMARY KEY (id),
  CONSTRAINT accounting_piece_lines_accountId_fkey FOREIGN KEY (accountId) REFERENCES public.chart_of_accounts(id),
  CONSTRAINT accounting_piece_lines_pieceId_fkey FOREIGN KEY (pieceId) REFERENCES public.accounting_pieces(id)
);
CREATE TABLE public.accounting_pieces (
  id text NOT NULL,
  schoolId text NOT NULL,
  academicYearId text,
  pieceNumber text NOT NULL,
  journalCode text NOT NULL,
  journalType USER-DEFINED NOT NULL,
  pieceDate timestamp without time zone NOT NULL,
  description text NOT NULL,
  reference text,
  documentType text,
  documentRef text,
  documentUrl text,
  totalDebit numeric NOT NULL DEFAULT 0,
  totalCredit numeric NOT NULL DEFAULT 0,
  isBalanced boolean NOT NULL DEFAULT false,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"PieceStatus",
  createdById text NOT NULL,
  validatedById text,
  validatedAt timestamp without time zone,
  postedById text,
  postedAt timestamp without time zone,
  fiscalPeriod text,
  fiscalYear integer,
  expenseId text,
  paymentId text,
  invoiceId text,
  payslipId text,
  isReversing boolean NOT NULL DEFAULT false,
  reversedPieceId text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT accounting_pieces_pkey PRIMARY KEY (id),
  CONSTRAINT accounting_pieces_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT accounting_pieces_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.announcements (
  id text NOT NULL,
  school_id text NOT NULL,
  title text NOT NULL,
  content text NOT NULL,
  type USER-DEFINED NOT NULL,
  priority USER-DEFINED NOT NULL DEFAULT 'normal'::"MessagePriority",
  start_date timestamp without time zone NOT NULL,
  end_date timestamp without time zone,
  is_pinned boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  view_count integer NOT NULL DEFAULT 0,
  targets ARRAY DEFAULT ARRAY['all'::text],
  target_class_ids ARRAY DEFAULT ARRAY[]::text[],
  target_level_ids ARRAY DEFAULT ARRAY[]::text[],
  attachments jsonb,
  academic_year_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  created_by_id text NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT announcements_pkey PRIMARY KEY (id),
  CONSTRAINT announcements_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT announcements_createdById_fkey FOREIGN KEY (created_by_id) REFERENCES public.users(id),
  CONSTRAINT announcements_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.attendance_aggregations (
  id text NOT NULL,
  schoolId text NOT NULL,
  cycleId text,
  staffType text,
  periodType text NOT NULL,
  periodDate timestamp without time zone NOT NULL,
  periodKey text NOT NULL,
  totalStaff integer NOT NULL DEFAULT 0,
  present integer NOT NULL DEFAULT 0,
  absent integer NOT NULL DEFAULT 0,
  late integer NOT NULL DEFAULT 0,
  onLeave integer NOT NULL DEFAULT 0,
  onMission integer NOT NULL DEFAULT 0,
  sick integer NOT NULL DEFAULT 0,
  rate double precision NOT NULL DEFAULT 0,
  totalHours double precision NOT NULL DEFAULT 0,
  overtimeHours double precision NOT NULL DEFAULT 0,
  metadata jsonb,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT attendance_aggregations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.attendance_alerts (
  id text NOT NULL,
  schoolId text NOT NULL,
  staffId text NOT NULL,
  alertType text NOT NULL,
  severity text NOT NULL DEFAULT 'warning'::text,
  title text NOT NULL,
  description text NOT NULL,
  metadata jsonb,
  isRead boolean NOT NULL DEFAULT false,
  readAt timestamp without time zone,
  isResolved boolean NOT NULL DEFAULT false,
  resolvedAt timestamp without time zone,
  resolvedBy text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT attendance_alerts_pkey PRIMARY KEY (id),
  CONSTRAINT attendance_alerts_staffId_fkey FOREIGN KEY (staffId) REFERENCES public.staff(id)
);
CREATE TABLE public.attendance_policies (
  id text NOT NULL,
  schoolId text NOT NULL,
  cycleId text,
  staffType text,
  name text NOT NULL,
  workStartTime text NOT NULL DEFAULT '07:30'::text,
  workEndTime text NOT NULL DEFAULT '16:00'::text,
  lateThreshold integer NOT NULL DEFAULT 15,
  absentThreshold integer NOT NULL DEFAULT 120,
  requireCheckout boolean NOT NULL DEFAULT true,
  workDays jsonb NOT NULL DEFAULT '[1, 2, 3, 4, 5, 6]'::jsonb,
  breakStartTime text,
  breakEndTime text,
  isActive boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT attendance_policies_pkey PRIMARY KEY (id),
  CONSTRAINT attendance_policies_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.attendances (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text NOT NULL,
  class_id text NOT NULL,
  date date NOT NULL,
  status USER-DEFINED NOT NULL,
  arrival_time text,
  late_minutes integer NOT NULL DEFAULT 0,
  reason text,
  is_justified boolean NOT NULL DEFAULT false,
  justification_document text,
  time_slot_id text,
  recorded_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  academic_year_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT attendances_pkey PRIMARY KEY (id),
  CONSTRAINT attendances_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT attendances_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT attendances_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT attendances_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT attendances_timeSlotId_fkey FOREIGN KEY (time_slot_id) REFERENCES public.time_slots(id)
);
CREATE TABLE public.audit_logs (
  id text NOT NULL,
  schoolId text,
  userId text NOT NULL,
  action text NOT NULL,
  entity text NOT NULL,
  entityId text,
  changes jsonb,
  metadata jsonb,
  ipAddress text,
  userAgent text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  duration integer,
  errorMessage text,
  groupId text,
  method text,
  newData jsonb,
  oldData jsonb,
  path text,
  success boolean NOT NULL DEFAULT true,
  userEmail text,
  userRole text,
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.books (
  id text NOT NULL,
  school_id text NOT NULL,
  isbn text NOT NULL,
  title text NOT NULL,
  author text,
  publisher text,
  publication_year integer,
  category text,
  description text,
  copies integer NOT NULL DEFAULT 1,
  available_copies integer NOT NULL DEFAULT 1,
  location text,
  cover_image text,
  tags jsonb NOT NULL DEFAULT '[]'::jsonb,
  status text NOT NULL DEFAULT 'available'::text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT books_pkey PRIMARY KEY (id),
  CONSTRAINT books_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.budget_lines (
  id text NOT NULL,
  budgetId text NOT NULL,
  code text NOT NULL,
  name text NOT NULL,
  description text,
  category USER-DEFINED NOT NULL,
  plannedAmount numeric NOT NULL,
  allocatedAmount numeric NOT NULL DEFAULT 0,
  spentAmount numeric NOT NULL DEFAULT 0,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT budget_lines_pkey PRIMARY KEY (id),
  CONSTRAINT budget_lines_budgetId_fkey FOREIGN KEY (budgetId) REFERENCES public.budgets(id)
);
CREATE TABLE public.budget_revisions (
  id text NOT NULL,
  budgetId text NOT NULL,
  userId text NOT NULL,
  action text NOT NULL,
  changes jsonb,
  reason text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT budget_revisions_pkey PRIMARY KEY (id),
  CONSTRAINT budget_revisions_budgetId_fkey FOREIGN KEY (budgetId) REFERENCES public.budgets(id),
  CONSTRAINT budget_revisions_userId_fkey FOREIGN KEY (userId) REFERENCES public.users(id)
);
CREATE TABLE public.budgets (
  id text NOT NULL,
  school_id text NOT NULL,
  academic_year_id text NOT NULL,
  name text NOT NULL,
  description text,
  fiscal_year text NOT NULL,
  total_amount numeric NOT NULL,
  allocated_amount numeric NOT NULL DEFAULT 0,
  spent_amount numeric NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'XAF'::text,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"BudgetStatus",
  approved_by_id text,
  approved_at timestamp without time zone,
  start_date timestamp without time zone NOT NULL,
  end_date timestamp without time zone NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT budgets_pkey PRIMARY KEY (id),
  CONSTRAINT budgets_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT budgets_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.business_categories (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  description text,
  icon text,
  orderIndex integer NOT NULL DEFAULT 0,
  isCore boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT business_categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.cafeteria_registrations (
  id text NOT NULL,
  schoolId text NOT NULL,
  studentId text NOT NULL,
  academicYearId text NOT NULL,
  dietaryRestriction text,
  allergies jsonb NOT NULL DEFAULT '[]'::jsonb,
  medicalNotes text,
  emergencyContact text,
  isActive boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT cafeteria_registrations_pkey PRIMARY KEY (id),
  CONSTRAINT cafeteria_registrations_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT cafeteria_registrations_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id),
  CONSTRAINT cafeteria_registrations_studentId_fkey FOREIGN KEY (studentId) REFERENCES public.students(id)
);
CREATE TABLE public.chart_of_accounts (
  id text NOT NULL,
  code text NOT NULL,
  name text NOT NULL,
  classNumber integer NOT NULL,
  category text NOT NULL,
  type USER-DEFINED NOT NULL,
  parentCode text,
  isDetail boolean NOT NULL DEFAULT true,
  description text,
  isActive boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT chart_of_accounts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.class_change_history (
  id text NOT NULL,
  studentId text NOT NULL,
  fromClassId text,
  toClassId text NOT NULL,
  fromClassName text,
  toClassName text NOT NULL,
  reason text,
  changedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  changedBy text,
  CONSTRAINT class_change_history_pkey PRIMARY KEY (id),
  CONSTRAINT class_change_history_studentId_fkey FOREIGN KEY (studentId) REFERENCES public.students(id)
);
CREATE TABLE public.class_councils (
  id text NOT NULL,
  school_id text NOT NULL,
  class_id text NOT NULL,
  academic_year_id text NOT NULL,
  type text NOT NULL,
  status text NOT NULL DEFAULT 'scheduled'::text,
  scheduled_at timestamp without time zone NOT NULL,
  location text,
  agenda text,
  participants jsonb NOT NULL DEFAULT '[]'::jsonb,
  started_at timestamp without time zone,
  ended_at timestamp without time zone,
  minutes text,
  general_remarks text,
  created_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT class_councils_pkey PRIMARY KEY (id),
  CONSTRAINT class_councils_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT class_councils_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT class_councils_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.class_promotions (
  id text NOT NULL,
  studentId text NOT NULL,
  fromClassId text NOT NULL,
  toClassId text,
  fromYearId text NOT NULL,
  toYearId text NOT NULL,
  decision USER-DEFINED NOT NULL,
  finalAverage double precision,
  finalRank integer,
  councilDate timestamp without time zone,
  councilComment text,
  processedAt timestamp without time zone,
  processedById text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT class_promotions_pkey PRIMARY KEY (id),
  CONSTRAINT class_promotions_fromClassId_fkey FOREIGN KEY (fromClassId) REFERENCES public.classes(id),
  CONSTRAINT class_promotions_fromYearId_fkey FOREIGN KEY (fromYearId) REFERENCES public.academic_years(id),
  CONSTRAINT class_promotions_studentId_fkey FOREIGN KEY (studentId) REFERENCES public.students(id),
  CONSTRAINT class_promotions_toClassId_fkey FOREIGN KEY (toClassId) REFERENCES public.classes(id),
  CONSTRAINT class_promotions_toYearId_fkey FOREIGN KEY (toYearId) REFERENCES public.academic_years(id)
);
CREATE TABLE public.classes (
  id text NOT NULL,
  school_id text NOT NULL,
  name text NOT NULL,
  section text,
  capacity integer NOT NULL DEFAULT 50,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  academic_year_id text NOT NULL,
  level_id text NOT NULL,
  main_teacher_id text,
  filiere_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT classes_pkey PRIMARY KEY (id),
  CONSTRAINT classes_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT classes_filiereId_fkey FOREIGN KEY (filiere_id) REFERENCES public.filieres(id),
  CONSTRAINT classes_levelId_fkey FOREIGN KEY (level_id) REFERENCES public.grade_levels(id),
  CONSTRAINT classes_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.configuration_audits (
  id text NOT NULL,
  entityType text NOT NULL,
  entityId text NOT NULL,
  action text NOT NULL,
  oldValue jsonb,
  newValue jsonb,
  userId text NOT NULL,
  userEmail text NOT NULL,
  reason text,
  metadata jsonb,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT configuration_audits_pkey PRIMARY KEY (id)
);
CREATE TABLE public.configuration_templates (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  description text,
  type text NOT NULL,
  config jsonb NOT NULL,
  isDefault boolean NOT NULL DEFAULT false,
  isSystem boolean NOT NULL DEFAULT false,
  createdBy text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT configuration_templates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.council_decisions (
  id text NOT NULL,
  council_id text NOT NULL,
  student_id text NOT NULL,
  decision text NOT NULL,
  comment text,
  has_congratulations boolean NOT NULL DEFAULT false,
  has_encouragements boolean NOT NULL DEFAULT false,
  has_work_warning boolean NOT NULL DEFAULT false,
  has_conduct_warning boolean NOT NULL DEFAULT false,
  destination_class_id text,
  recorded_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT council_decisions_pkey PRIMARY KEY (id),
  CONSTRAINT council_decisions_councilId_fkey FOREIGN KEY (council_id) REFERENCES public.class_councils(id),
  CONSTRAINT council_decisions_destinationClassId_fkey FOREIGN KEY (destination_class_id) REFERENCES public.classes(id),
  CONSTRAINT council_decisions_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.discipline_ledger (
  id text NOT NULL,
  schoolId text NOT NULL,
  entityId text NOT NULL,
  mutationId text,
  action USER-DEFINED NOT NULL,
  payloadBefore jsonb,
  payloadAfter jsonb NOT NULL,
  previousHash text NOT NULL DEFAULT 'GENESIS'::text,
  currentHash text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdBy text NOT NULL,
  resolvedBy text,
  CONSTRAINT discipline_ledger_pkey PRIMARY KEY (id)
);
CREATE TABLE public.disciplines (
  id text NOT NULL,
  school_id text NOT NULL,
  target_type USER-DEFINED NOT NULL DEFAULT 'student'::"DisciplineTargetType",
  student_id text,
  staff_id text,
  incident_type USER-DEFINED NOT NULL,
  description text NOT NULL,
  incident_date date NOT NULL,
  location text,
  witnesses text,
  sanction_type USER-DEFINED,
  severity USER-DEFINED NOT NULL,
  start_date date,
  end_date date,
  duration_days integer,
  parents_notified boolean NOT NULL DEFAULT false,
  parents_notified_at timestamp without time zone,
  is_executed boolean NOT NULL DEFAULT false,
  is_cancelled boolean NOT NULL DEFAULT false,
  cancellation_reason text,
  notes text,
  recorded_by_id text,
  status text NOT NULL DEFAULT 'reported'::text,
  resolved_at timestamp without time zone,
  resolved_by_id text,
  resolution text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  academic_year_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT disciplines_pkey PRIMARY KEY (id),
  CONSTRAINT disciplines_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT disciplines_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT disciplines_staffId_fkey FOREIGN KEY (staff_id) REFERENCES public.staff(id),
  CONSTRAINT disciplines_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.document_generation_requests (
  id text NOT NULL,
  schoolId text NOT NULL,
  type USER-DEFINED NOT NULL,
  templateId text,
  academicYearId text NOT NULL,
  classId text,
  studentIds ARRAY DEFAULT ARRAY[]::text[],
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"DocumentGenerationStatus",
  totalCount integer NOT NULL DEFAULT 0,
  processedCount integer NOT NULL DEFAULT 0,
  errorCount integer NOT NULL DEFAULT 0,
  errors jsonb,
  startedAt timestamp without time zone,
  completedAt timestamp without time zone,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdById text NOT NULL,
  CONSTRAINT document_generation_requests_pkey PRIMARY KEY (id),
  CONSTRAINT document_generation_requests_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT document_generation_requests_classId_fkey FOREIGN KEY (classId) REFERENCES public.classes(id),
  CONSTRAINT document_generation_requests_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.document_templates (
  id text NOT NULL,
  schoolId text,
  groupId text,
  type USER-DEFINED NOT NULL,
  name text NOT NULL,
  description text,
  content text NOT NULL,
  variables ARRAY DEFAULT ARRAY[]::text[],
  headerHtml text,
  footerHtml text,
  cssStyles text,
  logoUrl text,
  signatureUrl text,
  isDefault boolean NOT NULL DEFAULT false,
  isActive boolean NOT NULL DEFAULT true,
  version integer NOT NULL DEFAULT 1,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  createdById text NOT NULL,
  CONSTRAINT document_templates_pkey PRIMARY KEY (id),
  CONSTRAINT document_templates_groupId_fkey FOREIGN KEY (groupId) REFERENCES public.school_groups(id),
  CONSTRAINT document_templates_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.document_versions (
  id text NOT NULL,
  documentId text NOT NULL,
  version integer NOT NULL,
  fileUrl text NOT NULL,
  fileName text NOT NULL,
  fileSize integer NOT NULL,
  changes text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdById text NOT NULL,
  CONSTRAINT document_versions_pkey PRIMARY KEY (id),
  CONSTRAINT document_versions_documentId_fkey FOREIGN KEY (documentId) REFERENCES public.documents(id)
);
CREATE TABLE public.documents (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text,
  type USER-DEFINED NOT NULL,
  title text NOT NULL,
  description text,
  file_url text NOT NULL,
  file_name text NOT NULL,
  file_size integer NOT NULL,
  mime_type text NOT NULL,
  academic_year text,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"DocumentStatus",
  is_public boolean NOT NULL DEFAULT false,
  download_count integer NOT NULL DEFAULT 0,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  created_by_id text NOT NULL,
  category text,
  is_protected boolean NOT NULL DEFAULT false,
  level_id text,
  password text,
  shared_with ARRAY DEFAULT ARRAY[]::text[],
  tags ARRAY DEFAULT ARRAY[]::text[],
  visibility text,
  storage_path text,
  verification_code text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT documents_pkey PRIMARY KEY (id),
  CONSTRAINT documents_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT documents_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.evaluations (
  id text NOT NULL,
  school_id text NOT NULL,
  academic_year_id text NOT NULL,
  class_id text NOT NULL,
  subject_id text NOT NULL,
  teacher_id text,
  type USER-DEFINED NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"EvaluationStatus",
  title text NOT NULL,
  description text,
  maxValue numeric NOT NULL DEFAULT 20,
  coefficient integer NOT NULL DEFAULT 1,
  period text NOT NULL,
  scheduled_date timestamp without time zone,
  startTime text,
  endTime text,
  duration integer,
  averageScore numeric,
  minScore numeric,
  max_score numeric,
  passRate numeric,
  gradedCount integer NOT NULL DEFAULT 0,
  totalStudents integer NOT NULL DEFAULT 0,
  published_at timestamp without time zone,
  publishedById text,
  archivedAt timestamp without time zone,
  archivedById text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  created_by_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT evaluations_pkey PRIMARY KEY (id),
  CONSTRAINT evaluations_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT evaluations_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT evaluations_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT evaluations_subjectId_fkey FOREIGN KEY (subject_id) REFERENCES public.subjects(id)
);
CREATE TABLE public.expense_audits (
  id text NOT NULL,
  expenseId text NOT NULL,
  action text NOT NULL,
  userId text NOT NULL,
  oldData text,
  newData text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT expense_audits_pkey PRIMARY KEY (id),
  CONSTRAINT expense_audits_expenseId_fkey FOREIGN KEY (expenseId) REFERENCES public.expenses(id)
);
CREATE TABLE public.expenses (
  id text NOT NULL,
  school_id text NOT NULL,
  academic_year_id text,
  expenseNumber text NOT NULL,
  title text NOT NULL,
  description text,
  category USER-DEFINED NOT NULL,
  amount numeric NOT NULL,
  currency text NOT NULL DEFAULT 'XAF'::text,
  supplierId text,
  beneficiary text,
  beneficiaryType text,
  payment_method text,
  paymentReference text,
  paidAt timestamp without time zone,
  receipt_url text,
  invoiceUrl text,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"ExpenseStatus",
  approved_by_id text,
  approvedAt timestamp without time zone,
  rejectionReason text,
  budget_line_id text,
  expenseDate timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT expenses_pkey PRIMARY KEY (id),
  CONSTRAINT expenses_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT expenses_budgetLineId_fkey FOREIGN KEY (budget_line_id) REFERENCES public.budget_lines(id),
  CONSTRAINT expenses_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT expenses_supplierId_fkey FOREIGN KEY (supplierId) REFERENCES public.suppliers(id)
);
CREATE TABLE public.fees (
  id text NOT NULL,
  school_id text NOT NULL,
  academic_year_id text NOT NULL,
  name text NOT NULL,
  description text,
  amount numeric NOT NULL,
  currency text NOT NULL DEFAULT 'XAF'::text,
  type USER-DEFINED NOT NULL,
  class_id text,
  due_date timestamp without time zone,
  is_required boolean NOT NULL DEFAULT true,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  createdById text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT fees_pkey PRIMARY KEY (id),
  CONSTRAINT fees_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT fees_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT fees_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.filieres (
  id text NOT NULL,
  cycle_id text NOT NULL,
  name text NOT NULL,
  code text NOT NULL,
  description text,
  order_index integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  short_name text,
  CONSTRAINT filieres_pkey PRIMARY KEY (id),
  CONSTRAINT filieres_cycleId_fkey FOREIGN KEY (cycle_id) REFERENCES public.school_cycles(id)
);
CREATE TABLE public.financial_ledger (
  id text NOT NULL,
  schoolId text NOT NULL,
  entityId text NOT NULL,
  entityType text NOT NULL,
  mutationId text,
  action USER-DEFINED NOT NULL,
  payloadBefore jsonb,
  payloadAfter jsonb NOT NULL,
  previousHash text NOT NULL DEFAULT 'GENESIS'::text,
  currentHash text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdBy text NOT NULL,
  resolvedBy text,
  CONSTRAINT financial_ledger_pkey PRIMARY KEY (id)
);
CREATE TABLE public.financial_reports (
  id text NOT NULL,
  schoolId text NOT NULL,
  academicYearId text,
  type text NOT NULL,
  periodStart timestamp without time zone NOT NULL,
  periodEnd timestamp without time zone NOT NULL,
  totalInvoiced numeric NOT NULL,
  totalCollected numeric NOT NULL,
  totalPending numeric NOT NULL,
  totalOverdue numeric NOT NULL,
  collectionRate numeric NOT NULL,
  invoiceCount integer NOT NULL,
  paidInvoices integer NOT NULL,
  overdueInvoices integer NOT NULL,
  details jsonb NOT NULL DEFAULT '{}'::jsonb,
  generatedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT financial_reports_pkey PRIMARY KEY (id),
  CONSTRAINT financial_reports_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT financial_reports_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.fiscal_period_closes (
  id text NOT NULL,
  schoolId text NOT NULL,
  academicYearId text,
  fiscalPeriod text NOT NULL,
  periodStart timestamp without time zone NOT NULL,
  periodEnd timestamp without time zone NOT NULL,
  totalPayslips integer NOT NULL DEFAULT 0,
  totalGrossSalary numeric NOT NULL DEFAULT 0,
  totalDeductions numeric NOT NULL DEFAULT 0,
  totalNetSalary numeric NOT NULL DEFAULT 0,
  totalAccountingEntries integer NOT NULL DEFAULT 0,
  status text NOT NULL DEFAULT 'open'::text,
  closedBy text,
  closedAt timestamp without time zone,
  reopenedBy text,
  reopenedAt timestamp without time zone,
  reopenReason text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT fiscal_period_closes_pkey PRIMARY KEY (id),
  CONSTRAINT fiscal_period_closes_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT fiscal_period_closes_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.fiscal_years (
  id text NOT NULL,
  schoolId text NOT NULL,
  name text NOT NULL,
  startDate timestamp without time zone NOT NULL,
  endDate timestamp without time zone NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'open'::"FiscalYearStatus",
  closedAt timestamp without time zone,
  closedById text,
  netResult numeric,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT fiscal_years_pkey PRIMARY KEY (id)
);
CREATE TABLE public.grade_levels (
  id text NOT NULL,
  cycle_id text NOT NULL,
  name text NOT NULL,
  short_name text NOT NULL,
  order_index integer NOT NULL DEFAULT 0,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT grade_levels_pkey PRIMARY KEY (id),
  CONSTRAINT grade_levels_cycleId_fkey FOREIGN KEY (cycle_id) REFERENCES public.school_cycles(id)
);
CREATE TABLE public.grades (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text NOT NULL,
  subject_id text NOT NULL,
  class_id text NOT NULL,
  academic_year_id text NOT NULL,
  teacher_id text,
  type USER-DEFINED NOT NULL,
  value numeric NOT NULL,
  max_value numeric NOT NULL DEFAULT 20,
  coefficient integer NOT NULL DEFAULT 1,
  period text NOT NULL,
  evaluation_date timestamp without time zone,
  comment text,
  evaluation_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT grades_pkey PRIMARY KEY (id),
  CONSTRAINT grades_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT grades_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT grades_evaluationId_fkey FOREIGN KEY (evaluation_id) REFERENCES public.evaluations(id),
  CONSTRAINT grades_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT grades_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT grades_subjectId_fkey FOREIGN KEY (subject_id) REFERENCES public.subjects(id)
);
CREATE TABLE public.group_role_overrides (
  id text NOT NULL,
  group_id text NOT NULL,
  role USER-DEFINED NOT NULL,
  category_id text NOT NULL,
  canRead boolean,
  canWrite boolean,
  can_delete boolean,
  can_export boolean,
  disabled boolean NOT NULL DEFAULT false,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT group_role_overrides_pkey PRIMARY KEY (id),
  CONSTRAINT group_role_overrides_categoryId_fkey FOREIGN KEY (category_id) REFERENCES public.business_categories(id),
  CONSTRAINT group_role_overrides_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.school_groups(id)
);
CREATE TABLE public.holidays (
  id text NOT NULL,
  schoolId text,
  groupId text,
  name text NOT NULL,
  date timestamp without time zone NOT NULL,
  endDate timestamp without time zone,
  isNational boolean NOT NULL DEFAULT false,
  isRecurring boolean NOT NULL DEFAULT false,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT holidays_pkey PRIMARY KEY (id),
  CONSTRAINT holidays_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.infirmary_visits (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text NOT NULL,
  visit_type text NOT NULL,
  visit_date timestamp without time zone NOT NULL,
  reason text NOT NULL,
  symptoms jsonb NOT NULL DEFAULT '[]'::jsonb,
  diagnosis text,
  treatment text,
  medications_given jsonb NOT NULL DEFAULT '[]'::jsonb,
  temperature double precision,
  blood_pressure text,
  outcome text,
  parents_contacted boolean NOT NULL DEFAULT false,
  parent_contact_time text,
  follow_up_required boolean NOT NULL DEFAULT false,
  follow_up_date timestamp without time zone,
  notes text,
  recorded_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT infirmary_visits_pkey PRIMARY KEY (id),
  CONSTRAINT infirmary_visits_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT infirmary_visits_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.inscription_state_history (
  id text NOT NULL,
  inscriptionId text NOT NULL,
  fromState text NOT NULL,
  toState text NOT NULL,
  action text NOT NULL,
  reason text,
  metadata text,
  changedById text NOT NULL,
  changedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT inscription_state_history_pkey PRIMARY KEY (id),
  CONSTRAINT inscription_state_history_changedById_fkey FOREIGN KEY (changedById) REFERENCES public.users(id),
  CONSTRAINT inscription_state_history_inscriptionId_fkey FOREIGN KEY (inscriptionId) REFERENCES public.inscriptions(id)
);
CREATE TABLE public.inscriptions (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text,
  academic_year_id text NOT NULL,
  class_id text,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"InscriptionStatus",
  type USER-DEFINED NOT NULL DEFAULT 'new'::"InscriptionType",
  first_name text,
  last_name text,
  date_of_birth timestamp without time zone,
  gender USER-DEFINED,
  birth_place text,
  nationality text DEFAULT 'Congolaise'::text,
  parent_name text,
  parent_phone text,
  parent_email text,
  parent_address text,
  parent_profession text,
  previous_school text,
  previous_class text,
  transfer_reason text,
  documents jsonb,
  registration_fee numeric,
  is_paid boolean NOT NULL DEFAULT false,
  paid_at timestamp without time zone,
  payment_method USER-DEFINED,
  payment_ref text,
  validated_at timestamp without time zone,
  validated_by_id text,
  rejection_reason text,
  notes text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  created_by_id text,
  address text,
  amount_paid numeric,
  boarding_fee numeric,
  city text DEFAULT 'Brazzaville'::text,
  emergency_contact_name text,
  emergency_contact_phone text,
  emergency_contact_relation text,
  has_scholarship boolean NOT NULL DEFAULT false,
  has_social_aid boolean NOT NULL DEFAULT false,
  is_affecte boolean NOT NULL DEFAULT false,
  is_boarder boolean NOT NULL DEFAULT false,
  is_redoublant boolean NOT NULL DEFAULT false,
  level_id text,
  nombre_freres_soeurs integer DEFAULT 0,
  other_fees numeric,
  parent2_address text,
  parent2_email text,
  parent2_name text,
  parent2_phone text,
  parent2_profession text,
  parent2_relation text,
  parent_relation text,
  region text DEFAULT 'Brazzaville'::text,
  scholarship_type text,
  situation_familiale text,
  social_aid_type text,
  tuition_fee numeric,
  filiere_id text,
  photo_url text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  archived_at timestamp without time zone,
  is_archived boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT inscriptions_pkey PRIMARY KEY (id),
  CONSTRAINT inscriptions_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT inscriptions_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT inscriptions_filiereId_fkey FOREIGN KEY (filiere_id) REFERENCES public.filieres(id),
  CONSTRAINT inscriptions_levelId_fkey FOREIGN KEY (level_id) REFERENCES public.grade_levels(id),
  CONSTRAINT inscriptions_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT inscriptions_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.invoice_items (
  id text NOT NULL,
  invoiceId text NOT NULL,
  feeId text,
  description text NOT NULL,
  quantity integer NOT NULL DEFAULT 1,
  unitPrice numeric NOT NULL,
  discount numeric NOT NULL DEFAULT 0,
  total numeric NOT NULL,
  sortOrder integer NOT NULL DEFAULT 0,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT invoice_items_pkey PRIMARY KEY (id),
  CONSTRAINT invoice_items_feeId_fkey FOREIGN KEY (feeId) REFERENCES public.fees(id),
  CONSTRAINT invoice_items_invoiceId_fkey FOREIGN KEY (invoiceId) REFERENCES public.invoices(id)
);
CREATE TABLE public.invoice_notifications (
  id text NOT NULL,
  invoiceId text NOT NULL,
  type text NOT NULL,
  status text NOT NULL,
  error text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT invoice_notifications_pkey PRIMARY KEY (id),
  CONSTRAINT invoice_notifications_invoiceId_fkey FOREIGN KEY (invoiceId) REFERENCES public.invoices(id)
);
CREATE TABLE public.invoice_payments (
  id text NOT NULL,
  invoiceId text NOT NULL,
  amount numeric NOT NULL,
  method text NOT NULL,
  reference text,
  notes text,
  paidAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  recordedById text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT invoice_payments_pkey PRIMARY KEY (id),
  CONSTRAINT invoice_payments_invoiceId_fkey FOREIGN KEY (invoiceId) REFERENCES public.invoices(id)
);
CREATE TABLE public.invoices (
  id text NOT NULL,
  invoice_number text NOT NULL,
  school_id text NOT NULL,
  academic_year_id text NOT NULL,
  direction USER-DEFINED NOT NULL DEFAULT 'income'::"InvoiceDirection",
  student_id text,
  class_id text,
  supplier_id text,
  expense_id text,
  type USER-DEFINED NOT NULL DEFAULT 'tuition'::"InvoiceType",
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"InvoiceStatus",
  description text,
  items jsonb NOT NULL DEFAULT '[]'::jsonb,
  subtotal numeric NOT NULL DEFAULT 0,
  discount numeric NOT NULL DEFAULT 0,
  tax_rate numeric NOT NULL DEFAULT 0,
  tax_amount numeric NOT NULL DEFAULT 0,
  total numeric NOT NULL DEFAULT 0,
  paid_amount numeric NOT NULL DEFAULT 0,
  due_date timestamp without time zone,
  sent_at timestamp without time zone,
  paid_at timestamp without time zone,
  notes text,
  terms text,
  created_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT invoices_pkey PRIMARY KEY (id),
  CONSTRAINT invoices_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT invoices_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT invoices_expenseId_fkey FOREIGN KEY (expense_id) REFERENCES public.expenses(id),
  CONSTRAINT invoices_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT invoices_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT invoices_supplierId_fkey FOREIGN KEY (supplier_id) REFERENCES public.suppliers(id)
);
CREATE TABLE public.leave_requests (
  id text NOT NULL,
  schoolId text NOT NULL,
  staffId text NOT NULL,
  type text NOT NULL,
  startDate timestamp without time zone NOT NULL,
  endDate timestamp without time zone NOT NULL,
  halfDayStart boolean NOT NULL DEFAULT false,
  halfDayEnd boolean NOT NULL DEFAULT false,
  daysRequested double precision NOT NULL,
  reason text NOT NULL,
  attachmentUrl text,
  emergencyContact text,
  replacementStaffId text,
  status text NOT NULL DEFAULT 'pending'::text,
  managerComment text,
  requestedById text,
  processedById text,
  processedAt timestamp without time zone,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  deletedAt timestamp without time zone,
  isDeleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT leave_requests_pkey PRIMARY KEY (id),
  CONSTRAINT leave_requests_replacementStaffId_fkey FOREIGN KEY (replacementStaffId) REFERENCES public.staff(id),
  CONSTRAINT leave_requests_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id),
  CONSTRAINT leave_requests_staffId_fkey FOREIGN KEY (staffId) REFERENCES public.staff(id)
);
CREATE TABLE public.loans (
  id text NOT NULL,
  school_id text NOT NULL,
  book_id text NOT NULL,
  borrower_id text NOT NULL,
  borrower_type text NOT NULL,
  loan_date timestamp without time zone NOT NULL,
  due_date timestamp without time zone NOT NULL,
  return_date timestamp without time zone,
  return_condition text,
  status text NOT NULL DEFAULT 'active'::text,
  notes text,
  created_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT loans_pkey PRIMARY KEY (id),
  CONSTRAINT loans_bookId_fkey FOREIGN KEY (book_id) REFERENCES public.books(id),
  CONSTRAINT loans_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.meal_attendances (
  id text NOT NULL,
  schoolId text NOT NULL,
  studentId text NOT NULL,
  date timestamp without time zone NOT NULL,
  mealType text NOT NULL,
  attended boolean NOT NULL DEFAULT true,
  notes text,
  recordedById text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT meal_attendances_pkey PRIMARY KEY (id),
  CONSTRAINT meal_attendances_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id),
  CONSTRAINT meal_attendances_studentId_fkey FOREIGN KEY (studentId) REFERENCES public.students(id)
);
CREATE TABLE public.medical_records (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text NOT NULL,
  blood_type text,
  allergies jsonb NOT NULL DEFAULT '[]'::jsonb,
  chronic_conditions jsonb NOT NULL DEFAULT '[]'::jsonb,
  current_medications jsonb NOT NULL DEFAULT '[]'::jsonb,
  vaccinations jsonb NOT NULL DEFAULT '[]'::jsonb,
  doctor_contact text,
  emergency_contact text,
  emergency_phone text,
  notes text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT medical_records_pkey PRIMARY KEY (id),
  CONSTRAINT medical_records_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT medical_records_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.menus (
  id text NOT NULL,
  school_id text NOT NULL,
  date timestamp without time zone NOT NULL,
  meal_type text NOT NULL,
  starter text,
  main_course text,
  side_dish text,
  dessert text,
  beverage text,
  price double precision NOT NULL DEFAULT 0,
  allergens jsonb NOT NULL DEFAULT '[]'::jsonb,
  nutritional_info text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT menus_pkey PRIMARY KEY (id),
  CONSTRAINT menus_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.messages (
  id text NOT NULL,
  schoolId text NOT NULL,
  senderId text NOT NULL,
  type USER-DEFINED NOT NULL,
  subject text NOT NULL,
  content text NOT NULL,
  priority USER-DEFINED NOT NULL DEFAULT 'normal'::"MessagePriority",
  targetType USER-DEFINED NOT NULL,
  targetId text,
  isRead boolean NOT NULL DEFAULT false,
  readAt timestamp without time zone,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deletedAt timestamp without time zone,
  isDeleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT messages_pkey PRIMARY KEY (id),
  CONSTRAINT messages_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id),
  CONSTRAINT messages_senderId_fkey FOREIGN KEY (senderId) REFERENCES public.users(id)
);
CREATE TABLE public.module_dependencies (
  id text NOT NULL,
  module_id text NOT NULL,
  depends_on_id text NOT NULL,
  isOptional boolean NOT NULL DEFAULT false,
  description text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT module_dependencies_pkey PRIMARY KEY (id),
  CONSTRAINT module_dependencies_dependsOnId_fkey FOREIGN KEY (depends_on_id) REFERENCES public.modules(id),
  CONSTRAINT module_dependencies_moduleId_fkey FOREIGN KEY (module_id) REFERENCES public.modules(id)
);
CREATE TABLE public.modules (
  id text NOT NULL,
  category_id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  description text,
  icon text,
  is_core boolean NOT NULL DEFAULT false,
  requiredPlan USER-DEFINED NOT NULL DEFAULT 'gratuit'::"PlanType",
  status USER-DEFINED NOT NULL DEFAULT 'active'::"ModuleStatus",
  orderIndex integer NOT NULL DEFAULT 0,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT modules_pkey PRIMARY KEY (id),
  CONSTRAINT modules_categoryId_fkey FOREIGN KEY (category_id) REFERENCES public.business_categories(id)
);
CREATE TABLE public.notifications (
  id text NOT NULL,
  type USER-DEFINED NOT NULL,
  category USER-DEFINED NOT NULL,
  priority USER-DEFINED NOT NULL DEFAULT 'normal'::"NotificationPriority",
  title text NOT NULL,
  message text NOT NULL,
  data jsonb,
  user_id text,
  group_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  version integer NOT NULL DEFAULT 1,
  is_read boolean NOT NULL DEFAULT false,
  read_at timestamp with time zone,
  is_dismissed boolean NOT NULL DEFAULT false,
  dismissed_at timestamp with time zone,
  expires_at timestamp with time zone,
  is_broadcast boolean NOT NULL DEFAULT false,
  role_target text,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.school_groups(id),
  CONSTRAINT notifications_userId_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.payments (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text NOT NULL,
  fee_id text NOT NULL,
  amount numeric NOT NULL,
  currency text NOT NULL DEFAULT 'XAF'::text,
  method USER-DEFINED NOT NULL,
  reference text,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"PaymentStatus",
  paid_at timestamp without time zone,
  notes text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  created_by_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT payments_pkey PRIMARY KEY (id),
  CONSTRAINT payments_feeId_fkey FOREIGN KEY (fee_id) REFERENCES public.fees(id),
  CONSTRAINT payments_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT payments_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.payslip_audit_logs (
  id text NOT NULL,
  payslipId text NOT NULL,
  action text NOT NULL,
  oldValues jsonb,
  newValues jsonb,
  performedBy text NOT NULL,
  performedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  ipAddress text,
  userAgent text,
  reason text,
  CONSTRAINT payslip_audit_logs_pkey PRIMARY KEY (id),
  CONSTRAINT payslip_audit_logs_payslipId_fkey FOREIGN KEY (payslipId) REFERENCES public.payslips(id)
);
CREATE TABLE public.payslips (
  id text NOT NULL,
  school_id text NOT NULL,
  staff_id text NOT NULL,
  month integer NOT NULL,
  year integer NOT NULL,
  base_salary double precision NOT NULL,
  gross_salary double precision NOT NULL,
  total_deductions double precision NOT NULL,
  net_salary double precision NOT NULL,
  currency text NOT NULL DEFAULT 'XAF'::text,
  items jsonb NOT NULL,
  remarks text,
  status text NOT NULL DEFAULT 'draft'::text,
  payment_method text,
  payment_date timestamp without time zone,
  transaction_reference text,
  generated_by_id text,
  approved_by_id text,
  approved_at timestamp without time zone,
  paid_by_id text,
  accounting_locked boolean NOT NULL DEFAULT false,
  accounting_locked_at timestamp without time zone,
  accounting_locked_by text,
  fiscal_period text,
  budget_line_id text,
  pdf_url text,
  pdf_generated_at timestamp without time zone,
  signature_employer text,
  signature_employee text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT payslips_pkey PRIMARY KEY (id),
  CONSTRAINT payslips_budgetLineId_fkey FOREIGN KEY (budget_line_id) REFERENCES public.budget_lines(id),
  CONSTRAINT payslips_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT payslips_staffId_fkey FOREIGN KEY (staff_id) REFERENCES public.staff(id)
);
CREATE TABLE public.plan_categories (
  id text NOT NULL,
  plan_id text NOT NULL,
  category_id text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT plan_categories_pkey PRIMARY KEY (id),
  CONSTRAINT plan_categories_categoryId_fkey FOREIGN KEY (category_id) REFERENCES public.business_categories(id),
  CONSTRAINT plan_categories_planId_fkey FOREIGN KEY (plan_id) REFERENCES public.subscription_plans(id)
);
CREATE TABLE public.plan_module_exclusions (
  id text NOT NULL,
  plan_id text NOT NULL,
  module_id text NOT NULL,
  reason text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdBy text,
  CONSTRAINT plan_module_exclusions_pkey PRIMARY KEY (id),
  CONSTRAINT plan_module_exclusions_moduleId_fkey FOREIGN KEY (module_id) REFERENCES public.modules(id),
  CONSTRAINT plan_module_exclusions_planId_fkey FOREIGN KEY (plan_id) REFERENCES public.subscription_plans(id)
);
CREATE TABLE public.plan_modules (
  id text NOT NULL,
  plan_id text NOT NULL,
  module_id text NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT plan_modules_pkey PRIMARY KEY (id),
  CONSTRAINT plan_modules_moduleId_fkey FOREIGN KEY (module_id) REFERENCES public.modules(id),
  CONSTRAINT plan_modules_planId_fkey FOREIGN KEY (plan_id) REFERENCES public.subscription_plans(id)
);
CREATE TABLE public.plan_pricing (
  id text NOT NULL,
  plan_id text NOT NULL,
  billing_period USER-DEFINED NOT NULL,
  price numeric NOT NULL,
  discountPercent integer NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'XAF'::text,
  isActive boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT plan_pricing_pkey PRIMARY KEY (id),
  CONSTRAINT plan_pricing_planId_fkey FOREIGN KEY (plan_id) REFERENCES public.subscription_plans(id)
);
CREATE TABLE public.platform_settings (
  id text NOT NULL DEFAULT 'platform_settings'::text,
  platform_name text NOT NULL DEFAULT 'E-PILOT CONGO'::text,
  platform_email text NOT NULL DEFAULT 'contact@e-pilot.cg'::text,
  platform_phone text NOT NULL DEFAULT '+242 06 XXX XX XX'::text,
  platform_address text NOT NULL DEFAULT 'Brazzaville, Congo'::text,
  platform_logo text,
  session_timeout integer NOT NULL DEFAULT 30,
  two_factor_enabled boolean NOT NULL DEFAULT false,
  password_min_length integer NOT NULL DEFAULT 8,
  email_notifications boolean NOT NULL DEFAULT true,
  sms_notifications boolean NOT NULL DEFAULT false,
  alerts_enabled boolean NOT NULL DEFAULT true,
  currency text NOT NULL DEFAULT 'XAF'::text,
  tax_rate double precision NOT NULL DEFAULT 18,
  invoice_prefix text NOT NULL DEFAULT 'EP-'::text,
  updated_at timestamp without time zone NOT NULL,
  updated_by text,
  CONSTRAINT platform_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.recurring_expenses (
  id text NOT NULL,
  schoolId text NOT NULL,
  academicYearId text,
  title text NOT NULL,
  description text,
  category USER-DEFINED NOT NULL,
  amount numeric NOT NULL,
  beneficiary text,
  beneficiaryType text,
  budgetLineId text,
  frequency text NOT NULL,
  startDate timestamp without time zone NOT NULL,
  endDate timestamp without time zone,
  nextDueDate timestamp without time zone NOT NULL,
  lastProcessedDate timestamp without time zone,
  isActive boolean NOT NULL DEFAULT true,
  autoApprove boolean NOT NULL DEFAULT false,
  createdById text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT recurring_expenses_pkey PRIMARY KEY (id),
  CONSTRAINT recurring_expenses_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT recurring_expenses_budgetLineId_fkey FOREIGN KEY (budgetLineId) REFERENCES public.budget_lines(id),
  CONSTRAINT recurring_expenses_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.refresh_tokens (
  id text NOT NULL,
  user_id text NOT NULL,
  token text NOT NULL,
  expires_at timestamp without time zone NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  revoked_at timestamp without time zone,
  CONSTRAINT refresh_tokens_pkey PRIMARY KEY (id),
  CONSTRAINT refresh_tokens_userId_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.report_cards (
  id text NOT NULL,
  school_id text NOT NULL,
  student_id text NOT NULL,
  class_id text NOT NULL,
  academic_year_id text NOT NULL,
  period text NOT NULL,
  status text NOT NULL DEFAULT 'draft'::text,
  subject_grades jsonb NOT NULL,
  general_average double precision NOT NULL,
  class_average double precision NOT NULL,
  rank integer NOT NULL,
  total_students integer NOT NULL,
  council_comment text,
  main_teacher_comment text,
  director_comment text,
  council_decision text,
  has_congratulations boolean NOT NULL DEFAULT false,
  has_encouragements boolean NOT NULL DEFAULT false,
  has_work_warning boolean NOT NULL DEFAULT false,
  has_conduct_warning boolean NOT NULL DEFAULT false,
  generated_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  generated_by_id text,
  approved_at timestamp without time zone,
  approved_by_id text,
  published_at timestamp without time zone,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT report_cards_pkey PRIMARY KEY (id),
  CONSTRAINT report_cards_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT report_cards_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT report_cards_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT report_cards_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.role_profiles (
  id text NOT NULL,
  role USER-DEFINED NOT NULL,
  category_id text NOT NULL,
  canRead boolean NOT NULL DEFAULT true,
  canWrite boolean NOT NULL DEFAULT false,
  can_delete boolean NOT NULL DEFAULT false,
  can_export boolean NOT NULL DEFAULT false,
  isDefault boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT role_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT role_profiles_categoryId_fkey FOREIGN KEY (category_id) REFERENCES public.business_categories(id)
);
CREATE TABLE public.rooms (
  id text NOT NULL,
  schoolId text NOT NULL,
  name text NOT NULL,
  code text NOT NULL,
  capacity integer NOT NULL,
  type text NOT NULL,
  equipment jsonb DEFAULT '[]'::jsonb,
  building text,
  floor text,
  isActive boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT rooms_pkey PRIMARY KEY (id),
  CONSTRAINT rooms_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.school_access_profile_modules (
  id text NOT NULL,
  access_profile_code text NOT NULL,
  school_group_id text NOT NULL,
  school_id text NOT NULL,
  module_id text NOT NULL,
  canRead boolean NOT NULL DEFAULT true,
  canWrite boolean NOT NULL DEFAULT false,
  can_delete boolean NOT NULL DEFAULT false,
  can_export boolean NOT NULL DEFAULT false,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdById text,
  CONSTRAINT school_access_profile_modules_pkey PRIMARY KEY (id),
  CONSTRAINT school_access_profile_modules_accessProfileCode_schoolGrou_fkey FOREIGN KEY (access_profile_code) REFERENCES public.access_profiles(code),
  CONSTRAINT school_access_profile_modules_accessProfileCode_schoolGrou_fkey FOREIGN KEY (school_group_id) REFERENCES public.access_profiles(code),
  CONSTRAINT school_access_profile_modules_accessProfileCode_schoolGrou_fkey FOREIGN KEY (access_profile_code) REFERENCES public.access_profiles(school_group_id),
  CONSTRAINT school_access_profile_modules_accessProfileCode_schoolGrou_fkey FOREIGN KEY (school_group_id) REFERENCES public.access_profiles(school_group_id),
  CONSTRAINT school_access_profile_modules_moduleId_fkey FOREIGN KEY (module_id) REFERENCES public.modules(id),
  CONSTRAINT school_access_profile_modules_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.school_backups (
  id text NOT NULL,
  schoolId text NOT NULL,
  backupType USER-DEFINED NOT NULL DEFAULT 'incremental'::"BackupType",
  version bigint NOT NULL,
  storageUrl text NOT NULL,
  sizeBytes bigint NOT NULL,
  checksum text NOT NULL,
  encryptionMethod text NOT NULL DEFAULT 'AES-256-GCM'::text,
  encryptedKeyId text,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"BackupStatus",
  completedAt timestamp without time zone,
  expiresAt timestamp without time zone,
  recordCount integer NOT NULL DEFAULT 0,
  metadata jsonb,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT school_backups_pkey PRIMARY KEY (id)
);
CREATE TABLE public.school_cycle_assignments (
  id text NOT NULL,
  school_id text NOT NULL,
  cycle_id text NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT school_cycle_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT school_cycle_assignments_cycle_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.school_cycles(id),
  CONSTRAINT school_cycle_assignments_school_id_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.school_cycles (
  id text NOT NULL,
  name text NOT NULL,
  code text NOT NULL,
  order_index integer NOT NULL DEFAULT 0,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT school_cycles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.school_events (
  id text NOT NULL,
  school_id text NOT NULL,
  title text NOT NULL,
  description text,
  type USER-DEFINED NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'planned'::"SchoolEventStatus",
  start_date timestamp without time zone NOT NULL,
  end_date timestamp without time zone,
  isAllDay boolean NOT NULL DEFAULT false,
  location text,
  color text,
  targets ARRAY DEFAULT ARRAY['all'::text],
  targetClassIds ARRAY DEFAULT ARRAY[]::text[],
  targetLevelIds ARRAY DEFAULT ARRAY[]::text[],
  maxParticipants integer,
  academic_year_id text,
  attachments jsonb,
  createdById text NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT school_events_pkey PRIMARY KEY (id),
  CONSTRAINT school_events_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT school_events_createdById_fkey FOREIGN KEY (createdById) REFERENCES public.users(id),
  CONSTRAINT school_events_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.school_groups (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  email text,
  phone text,
  address text,
  city text,
  country text NOT NULL DEFAULT 'Congo'::text,
  logo text,
  max_schools integer NOT NULL DEFAULT 1,
  max_users_per_school integer NOT NULL DEFAULT 50,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  description text,
  founded_year integer,
  website text,
  department text,
  version integer NOT NULL DEFAULT 1,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT school_groups_pkey PRIMARY KEY (id)
);
CREATE TABLE public.school_sync_states (
  id text NOT NULL,
  schoolId text NOT NULL,
  lastSyncAt timestamp without time zone,
  lastFullSyncAt timestamp without time zone,
  lastSyncVersion bigint NOT NULL DEFAULT 0,
  totalRecords integer NOT NULL DEFAULT 0,
  pendingMutations integer NOT NULL DEFAULT 0,
  conflictsCount integer NOT NULL DEFAULT 0,
  dataChecksum text,
  syncStatus USER-DEFINED NOT NULL DEFAULT 'never_synced'::"SyncStatus",
  lastError text,
  isGenesisComplete boolean NOT NULL DEFAULT false,
  genesisCompletedAt timestamp without time zone,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  last_ingest_at timestamp with time zone,
  last_apply_at timestamp with time zone,
  backlog_depth integer DEFAULT 0,
  oldest_pending_mutation_at timestamp with time zone,
  failure_rate numeric DEFAULT 0,
  circuit_breaker_status text DEFAULT 'closed'::text,
  CONSTRAINT school_sync_states_pkey PRIMARY KEY (id)
);
CREATE TABLE public.schools (
  id text NOT NULL,
  group_id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  type USER-DEFINED NOT NULL,
  email text,
  phone text,
  address text,
  city text,
  logo text,
  director_id text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  description text,
  founded_year integer,
  motto text,
  principal_email text,
  principal_name text,
  principal_phone text,
  website text,
  school_code text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  province USER-DEFINED NOT NULL DEFAULT 'brazzaville'::"Province",
  CONSTRAINT schools_pkey PRIMARY KEY (id),
  CONSTRAINT schools_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.school_groups(id)
);
CREATE TABLE public.siblings (
  id text NOT NULL,
  student1_id text NOT NULL,
  student2_id text NOT NULL,
  relation USER-DEFINED NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT siblings_pkey PRIMARY KEY (id),
  CONSTRAINT siblings_student1Id_fkey FOREIGN KEY (student1_id) REFERENCES public.students(id),
  CONSTRAINT siblings_student2Id_fkey FOREIGN KEY (student2_id) REFERENCES public.students(id)
);
CREATE TABLE public.staff (
  id text NOT NULL,
  school_id text NOT NULL,
  user_id text,
  employee_id text NOT NULL,
  first_name text NOT NULL,
  last_name text NOT NULL,
  email text,
  phone text,
  date_of_birth timestamp without time zone,
  gender text,
  place_of_birth text,
  nationality text DEFAULT 'Congolaise'::text,
  marital_status text,
  address text,
  photo text,
  type text NOT NULL,
  position text NOT NULL,
  department text,
  city text,
  contract_type text NOT NULL,
  hire_date timestamp without time zone NOT NULL,
  contract_end_date timestamp without time zone,
  years_experience integer,
  previous_employer text,
  status text NOT NULL DEFAULT 'active'::text,
  base_salary double precision,
  currency text NOT NULL DEFAULT 'XAF'::text,
  social_security_number text,
  bank_account text,
  bank_name text,
  emergency_contact text,
  emergency_phone text,
  emergency_relation text,
  specializations text,
  qualifications jsonb NOT NULL DEFAULT '[]'::jsonb,
  experiences jsonb NOT NULL DEFAULT '[]'::jsonb,
  subject_ids jsonb NOT NULL DEFAULT '[]'::jsonb,
  cycle text,
  class_ids jsonb NOT NULL DEFAULT '[]'::jsonb,
  notes text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT staff_pkey PRIMARY KEY (id),
  CONSTRAINT staff_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT staff_userId_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.staff_attendances (
  id text NOT NULL,
  school_id text NOT NULL,
  staff_id text NOT NULL,
  date timestamp without time zone NOT NULL,
  status text NOT NULL,
  check_in_time text,
  check_out_time text,
  hours_worked double precision,
  overtime_hours double precision,
  remarks text,
  has_justification boolean NOT NULL DEFAULT false,
  justification_document text,
  recorded_by_id text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT staff_attendances_pkey PRIMARY KEY (id),
  CONSTRAINT staff_attendances_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT staff_attendances_staffId_fkey FOREIGN KEY (staff_id) REFERENCES public.staff(id)
);
CREATE TABLE public.staff_work_schedules (
  id text NOT NULL,
  schoolId text NOT NULL,
  staffId text NOT NULL,
  dayOfWeek integer NOT NULL,
  startTime text NOT NULL,
  endTime text NOT NULL,
  breakStart text,
  breakEnd text,
  isWorkDay boolean NOT NULL DEFAULT true,
  isActive boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT staff_work_schedules_pkey PRIMARY KEY (id),
  CONSTRAINT staff_work_schedules_staffId_fkey FOREIGN KEY (staffId) REFERENCES public.staff(id)
);
CREATE TABLE public.student_audit_logs (
  id text NOT NULL,
  studentId text NOT NULL,
  action text NOT NULL,
  fieldName text,
  oldValue text,
  newValue text,
  userId text,
  userEmail text,
  ipAddress text,
  userAgent text,
  metadata jsonb,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_audit_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.student_school_history (
  id text NOT NULL,
  studentId text NOT NULL,
  schoolId text NOT NULL,
  classId text,
  academicYearId text NOT NULL,
  entryDate timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  exitDate timestamp without time zone,
  entryReason USER-DEFINED NOT NULL DEFAULT 'new_inscription'::"EntryReason",
  exitReason USER-DEFINED,
  finalAverage double precision,
  finalRank integer,
  totalStudents integer,
  councilDecision text,
  observations text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_school_history_pkey PRIMARY KEY (id),
  CONSTRAINT student_school_history_academicYearId_fkey FOREIGN KEY (academicYearId) REFERENCES public.academic_years(id),
  CONSTRAINT student_school_history_classId_fkey FOREIGN KEY (classId) REFERENCES public.classes(id),
  CONSTRAINT student_school_history_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id),
  CONSTRAINT student_school_history_studentId_fkey FOREIGN KEY (studentId) REFERENCES public.students(id)
);
CREATE TABLE public.students (
  id text NOT NULL,
  school_id text NOT NULL,
  class_id text,
  matricule text NOT NULL,
  first_name text NOT NULL,
  last_name text NOT NULL,
  date_of_birth timestamp without time zone,
  gender text,
  address text,
  parent_phone text,
  parent_email text,
  photo text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT students_pkey PRIMARY KEY (id),
  CONSTRAINT students_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT students_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.subject_filiere_assignments (
  id text NOT NULL,
  subjectId text NOT NULL,
  filiereId text NOT NULL,
  coefficient integer,
  hoursPerWeek numeric,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT subject_filiere_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT subject_filiere_assignments_filiereId_fkey FOREIGN KEY (filiereId) REFERENCES public.filieres(id),
  CONSTRAINT subject_filiere_assignments_subjectId_fkey FOREIGN KEY (subjectId) REFERENCES public.subjects(id)
);
CREATE TABLE public.subject_level_assignments (
  id text NOT NULL,
  subjectId text NOT NULL,
  levelId text NOT NULL,
  hoursPerWeek numeric,
  coefficient integer,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT subject_level_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT subject_level_assignments_levelId_fkey FOREIGN KEY (levelId) REFERENCES public.grade_levels(id),
  CONSTRAINT subject_level_assignments_subjectId_fkey FOREIGN KEY (subjectId) REFERENCES public.subjects(id)
);
CREATE TABLE public.subject_teacher_assignments (
  id text NOT NULL,
  subjectId text NOT NULL,
  teacherId text NOT NULL,
  classId text,
  isPrimary boolean NOT NULL DEFAULT false,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT subject_teacher_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT subject_teacher_assignments_classId_fkey FOREIGN KEY (classId) REFERENCES public.classes(id),
  CONSTRAINT subject_teacher_assignments_subjectId_fkey FOREIGN KEY (subjectId) REFERENCES public.subjects(id),
  CONSTRAINT subject_teacher_assignments_teacherId_fkey FOREIGN KEY (teacherId) REFERENCES public.users(id)
);
CREATE TABLE public.subjects (
  id text NOT NULL,
  school_id text NOT NULL,
  name text NOT NULL,
  code text NOT NULL,
  coefficient integer NOT NULL DEFAULT 1,
  color text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  category USER-DEFINED,
  description text,
  hours_per_week numeric,
  is_optional boolean NOT NULL DEFAULT false,
  archived_at timestamp without time zone,
  is_national boolean NOT NULL DEFAULT false,
  national_reference_id text,
  parent_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT subjects_pkey PRIMARY KEY (id),
  CONSTRAINT subjects_nationalReferenceId_fkey FOREIGN KEY (national_reference_id) REFERENCES public.subjects(id),
  CONSTRAINT subjects_parentId_fkey FOREIGN KEY (parent_id) REFERENCES public.subjects(id),
  CONSTRAINT subjects_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);
CREATE TABLE public.subscription_invoices (
  id text NOT NULL,
  subscription_id text NOT NULL,
  groupId text NOT NULL,
  amount numeric NOT NULL,
  currency text NOT NULL DEFAULT 'XAF'::text,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::"InvoiceStatus",
  payment_method text,
  payment_reference text,
  transactionId text,
  paid_at timestamp without time zone,
  due_date timestamp without time zone NOT NULL,
  period_start timestamp without time zone NOT NULL,
  period_end timestamp without time zone NOT NULL,
  metadata jsonb,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  CONSTRAINT subscription_invoices_pkey PRIMARY KEY (id),
  CONSTRAINT subscription_invoices_groupId_fkey FOREIGN KEY (groupId) REFERENCES public.school_groups(id),
  CONSTRAINT subscription_invoices_subscriptionId_fkey FOREIGN KEY (subscription_id) REFERENCES public.subscriptions(id)
);
CREATE TABLE public.subscription_plans (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  type USER-DEFINED NOT NULL DEFAULT 'gratuit'::"PlanType",
  price numeric NOT NULL,
  currency text NOT NULL DEFAULT 'XAF'::text,
  max_schools integer NOT NULL,
  max_students integer NOT NULL,
  max_personnel integer NOT NULL,
  max_storage integer NOT NULL DEFAULT 1024,
  features jsonb,
  is_active boolean NOT NULL DEFAULT true,
  is_popular boolean NOT NULL DEFAULT false,
  order_index integer NOT NULL DEFAULT 0,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  billing_period USER-DEFINED NOT NULL DEFAULT 'monthly'::"BillingPeriod",
  CONSTRAINT subscription_plans_pkey PRIMARY KEY (id)
);
CREATE TABLE public.subscriptions (
  id text NOT NULL,
  group_id text NOT NULL,
  plan_id text NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"SubscriptionStatus",
  start_date timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  end_date timestamp without time zone,
  trial_ends_at timestamp without time zone,
  cancelled_at timestamp without time zone,
  cancelled_by text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  billing_period USER-DEFINED NOT NULL DEFAULT 'monthly'::"BillingPeriod",
  metadata jsonb,
  CONSTRAINT subscriptions_pkey PRIMARY KEY (id),
  CONSTRAINT subscriptions_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.school_groups(id),
  CONSTRAINT subscriptions_planId_fkey FOREIGN KEY (plan_id) REFERENCES public.subscription_plans(id)
);
CREATE TABLE public.suppliers (
  id text NOT NULL,
  schoolId text NOT NULL,
  name text NOT NULL,
  contactName text,
  phone text,
  email text,
  address text,
  taxId text,
  bankAccount text,
  notes text,
  isActive boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  deletedAt timestamp without time zone,
  isDeleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT suppliers_pkey PRIMARY KEY (id),
  CONSTRAINT suppliers_schoolId_fkey FOREIGN KEY (schoolId) REFERENCES public.schools(id)
);
CREATE TABLE public.support_tickets (
  id text NOT NULL,
  ticketNumber text NOT NULL,
  groupId text NOT NULL,
  createdById text NOT NULL,
  subject text NOT NULL,
  description text NOT NULL,
  category USER-DEFINED NOT NULL,
  priority USER-DEFINED NOT NULL DEFAULT 'normal'::"TicketPriority",
  status USER-DEFINED NOT NULL DEFAULT 'open'::"TicketStatus",
  assignedToId text,
  resolvedAt timestamp without time zone,
  resolvedById text,
  resolution text,
  satisfaction integer,
  firstResponseAt timestamp without time zone,
  lastActivityAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  deletedAt timestamp without time zone,
  isDeleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT support_tickets_pkey PRIMARY KEY (id),
  CONSTRAINT support_tickets_assignedToId_fkey FOREIGN KEY (assignedToId) REFERENCES public.users(id),
  CONSTRAINT support_tickets_createdById_fkey FOREIGN KEY (createdById) REFERENCES public.users(id),
  CONSTRAINT support_tickets_groupId_fkey FOREIGN KEY (groupId) REFERENCES public.school_groups(id),
  CONSTRAINT support_tickets_resolvedById_fkey FOREIGN KEY (resolvedById) REFERENCES public.users(id)
);
CREATE TABLE public.sync_conflicts (
  id text NOT NULL,
  schoolId text NOT NULL,
  mutationId text,
  entityType text NOT NULL,
  entityId text NOT NULL,
  clientValue jsonb NOT NULL,
  serverValue jsonb NOT NULL,
  clientUserId text NOT NULL,
  clientTimestamp timestamp without time zone NOT NULL,
  serverTimestamp timestamp without time zone NOT NULL,
  conflictType USER-DEFINED NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"ConflictStatus",
  resolution USER-DEFINED,
  resolvedValue jsonb,
  resolvedById text,
  resolvedAt timestamp without time zone,
  resolutionNote text,
  priority USER-DEFINED NOT NULL DEFAULT 'medium'::"ConflictPriority",
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  conflict_hash text,
  detected_by text,
  queue_message_id bigint,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT sync_conflicts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sync_health_snapshots (
  id text NOT NULL,
  groupId text,
  score integer NOT NULL,
  breakdown jsonb NOT NULL,
  totalSchools integer NOT NULL,
  syncedSchools integer NOT NULL,
  errorSchools integer NOT NULL,
  staleSchools integer NOT NULL,
  pendingConflicts integer NOT NULL,
  pendingMutations integer NOT NULL,
  successRate7d integer NOT NULL,
  avgDurationMs integer NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT sync_health_snapshots_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sync_logs (
  id text NOT NULL,
  schoolId text NOT NULL,
  eventType USER-DEFINED NOT NULL,
  mutationsCount integer NOT NULL DEFAULT 0,
  conflictsCount integer NOT NULL DEFAULT 0,
  duration integer,
  success boolean NOT NULL,
  errorMessage text,
  clientDeviceId text,
  clientVersion text,
  userAgent text,
  ipAddress text,
  userId text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT sync_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sync_mutations (
  id text NOT NULL,
  schoolId text NOT NULL,
  clientMutationId text NOT NULL,
  clientDeviceId text,
  entityType text NOT NULL,
  entityId text NOT NULL,
  operation USER-DEFINED NOT NULL,
  payload jsonb NOT NULL,
  previousState jsonb,
  version bigint NOT NULL DEFAULT nextval('sync_mutations_version_seq'::regclass),
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"MutationStatus",
  processedAt timestamp without time zone,
  errorMessage text,
  retryCount integer NOT NULL DEFAULT 0,
  userId text NOT NULL,
  clientCreatedAt timestamp without time zone NOT NULL,
  clientUpdatedAt timestamp without time zone,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  naturalKeyHash text,
  idempotency_key text,
  queue_message_id bigint,
  processed_by text,
  processing_started_at timestamp with time zone,
  applied_at timestamp with time zone,
  last_retry_at timestamp with time zone,
  failure_code text,
  operation_scope text DEFAULT 'entity'::text,
  entity_version bigint,
  server_received_at timestamp with time zone DEFAULT now(),
  source text DEFAULT 'powersync'::text,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT sync_mutations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.textbook_entries (
  id text NOT NULL,
  school_id text NOT NULL,
  class_id text NOT NULL,
  subject_id text NOT NULL,
  academic_year_id text NOT NULL,
  teacher_id text NOT NULL,
  type text NOT NULL,
  date timestamp without time zone NOT NULL,
  start_time text,
  end_time text,
  title text NOT NULL,
  content text,
  objectives text,
  homework text,
  homework_due_date timestamp without time zone,
  resources jsonb NOT NULL DEFAULT '[]'::jsonb,
  skills jsonb NOT NULL DEFAULT '[]'::jsonb,
  is_public boolean NOT NULL DEFAULT true,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  time_slot_id text,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT textbook_entries_pkey PRIMARY KEY (id),
  CONSTRAINT textbook_entries_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT textbook_entries_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT textbook_entries_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT textbook_entries_subjectId_fkey FOREIGN KEY (subject_id) REFERENCES public.subjects(id),
  CONSTRAINT textbook_entries_teacherId_fkey FOREIGN KEY (teacher_id) REFERENCES public.users(id),
  CONSTRAINT textbook_entries_timeSlotId_fkey FOREIGN KEY (time_slot_id) REFERENCES public.time_slots(id)
);
CREATE TABLE public.ticket_attachments (
  id text NOT NULL,
  ticketId text NOT NULL,
  fileName text NOT NULL,
  fileUrl text NOT NULL,
  fileSize integer NOT NULL,
  mimeType text NOT NULL,
  uploadedById text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT ticket_attachments_pkey PRIMARY KEY (id),
  CONSTRAINT ticket_attachments_ticketId_fkey FOREIGN KEY (ticketId) REFERENCES public.support_tickets(id),
  CONSTRAINT ticket_attachments_uploadedById_fkey FOREIGN KEY (uploadedById) REFERENCES public.users(id)
);
CREATE TABLE public.ticket_messages (
  id text NOT NULL,
  ticketId text NOT NULL,
  senderId text NOT NULL,
  content text NOT NULL,
  isInternal boolean NOT NULL DEFAULT false,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT ticket_messages_pkey PRIMARY KEY (id),
  CONSTRAINT ticket_messages_senderId_fkey FOREIGN KEY (senderId) REFERENCES public.users(id),
  CONSTRAINT ticket_messages_ticketId_fkey FOREIGN KEY (ticketId) REFERENCES public.support_tickets(id)
);
CREATE TABLE public.time_slots (
  id text NOT NULL,
  school_id text NOT NULL,
  class_id text NOT NULL,
  subject_id text NOT NULL,
  teacher_id text,
  academic_year_id text NOT NULL,
  day_of_week integer NOT NULL,
  start_time text NOT NULL,
  end_time text NOT NULL,
  room text,
  is_active boolean NOT NULL DEFAULT true,
  status USER-DEFINED NOT NULL DEFAULT 'DRAFT'::"TimeSlotStatus",
  validated_at timestamp without time zone,
  validated_by text,
  published_at timestamp without time zone,
  published_by text,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  version integer NOT NULL DEFAULT 1,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  CONSTRAINT time_slots_pkey PRIMARY KEY (id),
  CONSTRAINT time_slots_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT time_slots_classId_fkey FOREIGN KEY (class_id) REFERENCES public.classes(id),
  CONSTRAINT time_slots_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id),
  CONSTRAINT time_slots_subjectId_fkey FOREIGN KEY (subject_id) REFERENCES public.subjects(id)
);
CREATE TABLE public.transfer_audit_logs (
  id text NOT NULL,
  transferId text NOT NULL,
  action text NOT NULL,
  userId text NOT NULL,
  previousStatus text,
  newStatus text,
  comment text,
  metadata jsonb,
  ipAddress text,
  userAgent text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT transfer_audit_logs_pkey PRIMARY KEY (id),
  CONSTRAINT transfer_audit_logs_transferId_fkey FOREIGN KEY (transferId) REFERENCES public.transfers(id),
  CONSTRAINT transfer_audit_logs_userId_fkey FOREIGN KEY (userId) REFERENCES public.users(id)
);
CREATE TABLE public.transfers (
  id text NOT NULL,
  student_id text NOT NULL,
  origin_school_id text NOT NULL,
  origin_class_id text,
  destination_school_id text,
  destination_class_id text,
  academic_year_id text NOT NULL,
  type USER-DEFINED NOT NULL DEFAULT 'outgoing'::"TransferType",
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"TransferStatus",
  reason USER-DEFINED NOT NULL DEFAULT 'convenance_personnelle'::"TransferReason",
  reason_details text,
  request_date timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  approval_date_origin timestamp without time zone,
  approval_date_dest timestamp without time zone,
  effective_date timestamp without time zone,
  approved_by_origin_id text,
  approved_by_dest_id text,
  requested_by_id text NOT NULL,
  certificate_url text,
  documents jsonb,
  origin_comment text,
  dest_comment text,
  rejection_reason text,
  metadata jsonb,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  CONSTRAINT transfers_pkey PRIMARY KEY (id),
  CONSTRAINT transfers_academicYearId_fkey FOREIGN KEY (academic_year_id) REFERENCES public.academic_years(id),
  CONSTRAINT transfers_approvedByDestId_fkey FOREIGN KEY (approved_by_dest_id) REFERENCES public.users(id),
  CONSTRAINT transfers_approvedByOriginId_fkey FOREIGN KEY (approved_by_origin_id) REFERENCES public.users(id),
  CONSTRAINT transfers_destinationClassId_fkey FOREIGN KEY (destination_class_id) REFERENCES public.classes(id),
  CONSTRAINT transfers_destinationSchoolId_fkey FOREIGN KEY (destination_school_id) REFERENCES public.schools(id),
  CONSTRAINT transfers_originClassId_fkey FOREIGN KEY (origin_class_id) REFERENCES public.classes(id),
  CONSTRAINT transfers_originSchoolId_fkey FOREIGN KEY (origin_school_id) REFERENCES public.schools(id),
  CONSTRAINT transfers_requestedById_fkey FOREIGN KEY (requested_by_id) REFERENCES public.users(id),
  CONSTRAINT transfers_studentId_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.user_modules (
  id text NOT NULL,
  user_id text NOT NULL,
  module_id text NOT NULL,
  can_delete boolean NOT NULL DEFAULT false,
  can_export boolean NOT NULL DEFAULT false,
  can_read boolean NOT NULL DEFAULT true,
  can_write boolean NOT NULL DEFAULT false,
  assigned_at timestamp with time zone NOT NULL DEFAULT now(),
  assigned_by uuid,
  disabled_at timestamp with time zone,
  disabled_by uuid,
  CONSTRAINT user_modules_pkey PRIMARY KEY (id),
  CONSTRAINT user_modules_moduleId_fkey FOREIGN KEY (module_id) REFERENCES public.modules(id),
  CONSTRAINT user_modules_userId_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
  id text NOT NULL,
  email text NOT NULL,
  password_hash text,
  first_name text NOT NULL,
  last_name text NOT NULL,
  phone text,
  role USER-DEFINED NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::"UserStatus",
  gender USER-DEFINED,
  date_of_birth timestamp without time zone,
  group_id text,
  school_id text,
  avatar text,
  access_profile_code text,
  last_login_at timestamp without time zone,
  email_verified_at timestamp without time zone,
  is_sandbox boolean NOT NULL DEFAULT false,
  created_at timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone NOT NULL,
  address text,
  birth_place text,
  reset_token text,
  reset_token_expiry timestamp without time zone,
  login_attempts integer NOT NULL DEFAULT 0,
  locked_until timestamp without time zone,
  phone_verified_at timestamp without time zone,
  deleted_at timestamp without time zone,
  is_deleted boolean NOT NULL DEFAULT false,
  version integer NOT NULL DEFAULT 1,
  must_change_password boolean NOT NULL DEFAULT false,
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_accessProfileCode_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.access_profiles(code),
  CONSTRAINT users_accessProfileCode_groupId_fkey FOREIGN KEY (access_profile_code) REFERENCES public.access_profiles(code),
  CONSTRAINT users_accessProfileCode_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.access_profiles(school_group_id),
  CONSTRAINT users_accessProfileCode_groupId_fkey FOREIGN KEY (access_profile_code) REFERENCES public.access_profiles(school_group_id),
  CONSTRAINT users_groupId_fkey FOREIGN KEY (group_id) REFERENCES public.school_groups(id),
  CONSTRAINT users_schoolId_fkey FOREIGN KEY (school_id) REFERENCES public.schools(id)
);